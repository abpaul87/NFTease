import Rails from '@rails/ujs';

const autoPlay = () => {
  // const list = document.querySelector('#audio');
  // console.log(list)
  // list.play();
  // var isChrome = /Chrome/.test(navigator.userAgent) && /Google Inc/.test(navigator.vendor);
  // console.log(isChrome)
  // if (!isChrome) {
  //   document.getElementById('iframeAudio').remove()
  // }
  // else {
  //   document.getElementById('audio').remove() // just to make sure that it will not have 2x audio in the background
  // }
};

export { autoPlay };
